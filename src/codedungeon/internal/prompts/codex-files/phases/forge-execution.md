# Phase 5: Execution

- Implement tasks in dependency order.
- Use tests to drive behavior changes.
- Keep edits scoped.
- For each repo/PR, run `codedungeon code-review --out .codedungeon/code-review/<repo> --url <PR URL> --project-context .codedungeon/project-rules.compact.md --task-context .codedungeon/tasks/<feature>/<repo>/PLAN.md --post`.
- Record changed artifacts and commands.
- Close with `PHASE_5_COMPLETE`.
