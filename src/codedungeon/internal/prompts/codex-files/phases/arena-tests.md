# Phase 6: Test Arena

- Run the agreed verification commands.
- In multi-repo workflows, run QA sequentially per repo with `codedungeon qa run --cwd <repo> --phase 6 --fresh --cmd "<first cmd>"`.
- Capture failures exactly.
- Do not claim completion without passing output.
- Close with `PHASE_6_COMPLETE`.
